{
  "name": "rhanka",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "test": "node --test",
    "stats": "node scripts/github-profile-stats.mjs --write",
    "stats:dispatch": "gh workflow run profile-stats.yml"
  }
}
